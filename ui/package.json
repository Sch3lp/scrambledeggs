{
  "name": "ui",
  "version": "1.0.0",
  "description": "Contains all Elm source files.",
  "main": "index.js",
  "scripts": {
    "start": "elm-live src/main/elm/Main.elm --open -d src/main/resources --start-page=index.html --pushstate --proxy-host=http://localhost:8080/api --proxy-prefix=/api -- --output=src/main/resources/main.js --debug",
    "test": "elm-test src/test/**/*Test.elm"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-live": "^4.0.0-rc.1",
    "elm-test": "^0.19.1-revision6"
  }
}
